from flask import Flask, request

app = Flask(__name__)

BOT_TOKEN = "8946302400:AAFly9ODzxL1Cqj8EzI_-MatGpHMgWPUjJA"
ADMIN_CHAT_ID = 6811058523

HTML = """
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Домашняя Flora21</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #2d6a4f;
            min-height: 100vh;
            padding: 20px;
        }
        .container { max-width: 500px; margin: 0 auto; }
        h1 { color: white; text-align: center; margin-bottom: 20px; }
        .card {
            background: white;
            border-radius: 20px;
            padding: 20px;
            margin-bottom: 20px;
        }
        .card h2 { color: #2d6a4f; margin-bottom: 15px; }
        .bouquet-list { list-style: none; }
        .bouquet-item {
            padding: 12px 0;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
        }
        .bouquet-price { color: #ff6b8a; font-weight: bold; }
        button {
            background: #ff6b8a;
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 30px;
            width: 100%;
            cursor: pointer;
            margin-top: 10px;
            font-size: 16px;
        }
        input, select, textarea {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border: 1px solid #ddd;
            border-radius: 12px;
        }
        .order-form { display: none; }
        .order-form.active { display: block; }
        .back-btn { background: #ddd; color: #333; margin-top: 5px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>🌺 Домашняя Flora21</h1>
        
        <div id="catalog" class="card">
            <h2>Наши букеты</h2>
            <ul class="bouquet-list">
                <li class="bouquet-item"><span>🌹 Классические розы</span><span class="bouquet-price">1 500 ₽</span></li>
                <li class="bouquet-item"><span>🌸 Нежные пионы</span><span class="bouquet-price">2 000 ₽</span></li>
                <li class="bouquet-item"><span>🌼 Полевые цветы</span><span class="bouquet-price">1 200 ₽</span></li>
                <li class="bouquet-item"><span>🌻 Солнечный букет</span><span class="bouquet-price">1 600 ₽</span></li>
            </ul>
            <button onclick="showOrderForm()">📦 Оформить заказ</button>
        </div>
        
        <div id="orderForm" class="card order-form">
            <h2>Оформление заказа</h2>
            <form id="orderFormElement">
                <input type="text" id="name" placeholder="Ваше имя" required>
                <input type="tel" id="phone" placeholder="Телефон" required>
                <select id="bouquet">
                    <option>Розы - 1500₽</option>
                    <option>Пионы - 2000₽</option>
                    <option>Ромашки - 1200₽</option>
                </select>
                <textarea id="address" rows="2" placeholder="Адрес доставки" required></textarea>
                <input type="text" id="datetime" placeholder="Дата и время">
                <textarea id="comment" rows="2" placeholder="Комментарий"></textarea>
                <button type="button" onclick="submitOrder()">📨 Отправить заказ</button>
                <button type="button" class="back-btn" onclick="showCatalog()">← Назад</button>
            </form>
        </div>
    </div>
    
    <script>
        function showOrderForm() {
            document.getElementById('catalog').style.display = 'none';
            document.getElementById('orderForm').classList.add('active');
        }
        function showCatalog() {
            document.getElementById('catalog').style.display = 'block';
            document.getElementById('orderForm').classList.remove('active');
        }
        async function submitOrder() {
            const name = document.getElementById('name').value;
            const phone = document.getElementById('phone').value;
            const bouquet = document.getElementById('bouquet').value;
            const address = document.getElementById('address').value;
            const datetime = document.getElementById('datetime').value;
            const comment = document.getElementById('comment').value;
            
            if (!name || !phone || !address) {
                alert('Заполните имя, телефон и адрес!');
                return;
            }
            
            const response = await fetch('/send_order', {
                method: 'POST',
                headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
                body: new URLSearchParams({ name, phone, bouquet, address, datetime, comment })
            });
            
            if (response.ok) {
                alert('✅ Заказ отправлен!');
                showCatalog();
                document.getElementById('orderFormElement').reset();
            }
        }
    </script>
</body>
</html>
"""

@app.route('/')
def home():
    return HTML

@app.route('/send_order', methods=['POST'])
def send_order():
    import requests
    name = request.form.get('name')
    phone = request.form.get('phone')
    bouquet = request.form.get('bouquet')
    address = request.form.get('address')
    datetime = request.form.get('datetime', 'Не указано')
    comment = request.form.get('comment', '')
    
    message = f"🌸 НОВЫЙ ЗАКАЗ!\n\nИмя: {name}\nТелефон: {phone}\nБукет: {bouquet}\nАдрес: {address}\nВремя: {datetime}\nКомментарий: {comment}"
    
    try:
        requests.post(
            f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage",
            json={"chat_id": ADMIN_CHAT_ID, "text": message}
        )
    except:
        pass
    
    return "OK", 200

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)